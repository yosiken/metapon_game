# METAPON（メタポン） ゲーム企画書 / 開発仕様書

| 項目 | 内容 |
| --- | --- |
| タイトル（仮） | METAPON（メタポン） |
| ジャンル | 落ち物アクションパズル（打ち上げ型 × アクティブ連鎖） |
| プラットフォーム | スマートフォンのブラウザ（PWA対応、縦画面固定） |
| プレイ人数 | 1人（ソロ専用） |
| 盤面 | **6列 × 12行 固定** |
| 1プレイ時間 | 60秒 〜 10分（モード依存） |
| 想定開発規模 | 個人〜小規模（コア検証2週 + 本開発4〜6週） |
| ドキュメント版数 | v2.0 |

---

## 1. コンセプト

> **「打ち上げてる最中が、いちばん忙しい。」**

ブロックを3つ揃えると、そのブロックが**燃料**になって、上に乗っている塊ごと画面外へ打ち上がる。
ただし**最初の一発だけでは、ほぼ届かない。**

上昇中の塊はまだ操作できる。落下中の塊も操作できる。
飛んでいる最中に次のマッチを作り、推力を継ぎ足し、連鎖を伸ばして画面外まで押し切る——
**「マッチさせて終わり」ではなく「マッチさせてからが本番」**のアクションパズル。

### 1.1 元ネタとの関係

| 元ネタ | 本作が継承するもの | 本作が変えるもの |
| --- | --- | --- |
| **メテオス** | 縦ドラッグ操作、3マッチ点火、推力 vs 重量の打ち上げ物理、失速落下 | 横10列 → **縦画面向けの6列**。打ち上げを「たまに起きる爽快演出」ではなく**常時の主戦場**に |
| **パネルでポン** | アクティブ連鎖（動いている最中に次を仕込む） | 連鎖の舞台を平面の盤面ではなく**上昇中／落下中の塊の内部**に移す |

「メテオスの操作感」に「パネポンの連鎖の緊張感」を接続するのが本作の設計主題。

---

## 2. ターゲットと前提

### 2.1 ターゲット

- **主**: 落ち物パズル経験者（ぷよ / パネポン / テトリス）で、スマホで腰を据えて上達したい層
- **副**: 短時間高密度のスコアアタックを好むカジュアル層

### 2.2 技術前提

| 項目 | 目標値 |
| --- | --- |
| 対応端末 | iPhone SE(2nd) / Snapdragon 6xx 相当以上 |
| ブラウザ | iOS Safari 16+, Chrome for Android 最新 |
| フレームレート | 60fps 固定（ロジックは60Hz固定ステップ） |
| 入力遅延 | タッチ〜描画反映 50ms 以内 |
| 初回ロード | 3G相当で3秒以内 / バンドル 500KB gzip 以内 |
| オフライン | PWA（Service Worker）で完全オフライン動作 |
| 画面向き | 縦固定（横向き時はローテーション促し画面） |

---

## 3. 画面構成

### 3.1 レイアウト

```
┌─────────────────────────────────┐
│  SCORE 128,400          Lv.5    │ ← HUD上部（画面高の 10%）
│  CHAIN ×4     THRUST ▮▮▮▮▯      │
├─────────────────────────────────┤
│ ╌╌╌╌╌╌╌╌╌ 排出ライン ╌╌╌╌╌╌╌╌╌╌ │ ← row 12 の上端。越えた塊は消滅＝得点
│  ┌───┬───┬───┬───┬───┬───┐      │
│  │   │   │   │   │   │   │ row11│ ← 危険域（赤の斜線表示）
│  │   │   │   │   │   │   │ row10│ ← 危険域
│  │   │   │   │   │   │   │ row 9│
│  │   │   │ ■ │   │   │   │ row 8│
│  │ ■ │   │ ● │   │   │ ● │ row 7│
│  │ ● │   │ ▲ │ ● │   │ ▲ │ row 6│
│  │ ▲ │ ● │ ■ │ ▲ │   │ ■ │ row 5│
│  │ ■ │ ▲ │ ● │ ■ │ ▲ │ ● │ row 4│
│  │ ● │ ■ │ ▲ │ ● │ ■ │ ▲ │ row 3│
│  │ ▲ │ ● │ ■ │ ▲ │ ● │ ■ │ row 2│
│  │ ■ │ ▲ │ ● │ ■ │ ▲ │ ● │ row 1│
│  │ ● │ ■ │ ▲ │ ● │ ■ │ ▲ │ row 0│
│  └───┴───┴───┴───┴───┴───┘      │
├─────────────────────────────────┤
│                                 │ ← 親指セーフゾーン（画面高の 12%）
│          [‖ PAUSE]              │   ここには操作対象を置かない
└─────────────────────────────────┘
```

### 3.2 レイアウト規約

- 盤面は論理グリッド **6列 × 12行**（+ 天井バッファ2行）。列数は固定であり、プレイヤーも開発者も変更しない。
- セルサイズは `cell = floor(min(vw * 0.94 / 6, (vh * 0.78) / ROWS))` で算出し、盤面を水平中央・垂直中央に配置。
- 余白は背景演出（宇宙／惑星）に使用。**操作UIを盤面の左右に置かない**（6列では盤面が画面幅の 70〜87% を占め、左右に余地がない）。
- HUDは盤面と重ねない。連鎖数だけは盤面中央にフローティング表示（大きく、0.4秒でフェード）。
- `touch-action: none` / `overscroll-behavior: none` / `user-select: none` を盤面要素に適用し、ブラウザのスクロール・引っ張り更新を抑止。

### 3.3 端末幅への適合

`cell = floor(min(vw * 0.94 / 6, (vh * 0.78) / ROWS))` において、**縦長端末では高さ側が律速**になる。
6列でもセルは幅の制約を受けず、タッチターゲット 40px を確保できる。

| 端末（論理px） | 律速 | セル | 盤面幅 | 画面幅に占める割合 |
| --- | --- | --- | --- | --- |
| iPhone SE(2) 375×667 | 高さ | 43 | 260 | 69% |
| iPhone 14 390×844 | 高さ | 54 | 329 | 84% |
| Android 360×800 | 高さ | 52 | 312 | 87% |
| iPhone 14 Pro Max 430×932 | 高さ | 60 | 362 | 84% |

幅側が律速になるのは `vw / vh > 0.62` の横長寄り端末のみ。その場合は下のフォールバックが働く。

**フォールバック規則**（列数は減らせないため、行数で吸収する）

1. `cell < 40px` → `ROWS` を 1 ずつ減らす（下限 10行）。
2. `ROWS = 10` でも `cell < 36px` → 「この端末では快適に遊べない可能性がある」と警告したうえで**プレイは許可する**（ブロックしない）。
3. `ROWS` が変わると `DANGER_ROW` は `ROWS - 2` に追従する。
4. **スコアは `ROWS` で正規化しない。** 行数が少ないほど不利なのは事実だが、稀なケースのために式を複雑にしない。

**両手持ち前提**

- 6列では両端の列が画面端に寄るため、**両手持ちが前提**。片手（親指のみ）では両端に届かない。
- 初回起動時に「両手で持ってください」を1回だけ提示する。
- **片手プレイ（歩行中・吊革）は本作の想定外**とする。6列固定の直接的な代償であり、承知のうえで選択している（15章）。

---

## 4. 基本ルール（メテオス準拠部分）

### 4.1 盤面とブロック供給

1. 盤面上空からブロックが降ってくる（`dropRate` 個/秒）。
2. 落ちたブロックは重力で最下段まで落ち、既存ブロックの上に積まれる。
3. ブロックは色属性を持つ（初期3色、難易度上昇で最大5色）。

### 4.2 点火（マッチ）

- **同色3つ以上**が **縦または横**に連続すると**点火**する。
- 点火したブロックは即座に消えるのではなく、`BURN_TIME` の間**燃焼状態**になり、**推力**を発生させる。
- 点火グループの**上に乗っているブロック全部**が、点火グループごと**1つの塊（スタック）**として持ち上がる。
- 横マッチが複数列にまたがる場合、その全列の上側ブロックが同一スタックに統合される。

### 4.3 打ち上げ物理

スタックは以下で毎フレーム積分される。

```
Ft = Σ(燃焼中グループ i の推力)            // グループが燃え尽きると 0 に戻る
Wt = Σ(スタック内全ブロックの重量)
a  = (Ft / Wt - G) * BASE_ACCEL            // [cell/s²]
v  = clamp(v + a * dt, -V_MAX_DOWN, V_MAX_UP)
y += v * dt
```

- 点火の瞬間、**初速インパルス**を加算: `v += IGNITE_IMPULSE * k / Wt`（`k` = 点火ブロック数）
- 燃焼グループは `BURN_TIME` 経過で**燃え尽きて消滅**（＝スタックの重量が減る／運動量は保持）。
- **排出**: スタックの**最下段ブロックが排出ラインを越えた**時点で、スタック全体が画面外へ消滅＝クリア。
- **失速落下**: `v` が 0 以下になり、排出ラインに届かなかった場合、スタックは落下して地上スタックの上に再着地する。

### 4.4 「一発では届かない」という設計意図

推力は点火数に比例し、重量はスタック全体に比例する。
つまり**下の方で点火するほど、上に載った重量を背負う**ため、**単発3マッチはほぼ確実に失速する。**

| 状況 | 推力 Ft | 重量 Wt | 加速度 a | 結果 |
| --- | --- | --- | --- | --- |
| 最上部で3マッチ（上に何もない） | 6.0 | 3.0 | +8.0 | 即排出 |
| 上に5個ある状態で3マッチ | 6.0 | 8.0 | −2.0 | 約3セル上昇して落下 |
| 上に5個 + 上昇中に2連鎖目 | 8.1 | 5.0 | +5.0 | 加速して排出 |

→ **「打ち上げてから連鎖を繋いで押し切る」ことが、クリアの標準手順**になる。

### 4.5 ゲームオーバー

- **地上スタック**（上昇/落下中の塊を除く）の高さが **`ROWS` 行を超えた状態**が `OVERFLOW_GRACE` 継続したらゲームオーバー。
- 高さ `DANGER_ROW`（= `ROWS - 2`）以上で**危険域演出**（盤面外周の赤パルス、BGMのフィルター、振動）。
- 上昇中のスタックは高さ判定に含めない（＝打ち上げ中は一時的に「安全」に見えるが、落ちてくると一気に危険になる）。

---

## 5. 追加ルール：アクティブ連鎖（本作の核）

### 5.1 上昇中・落下中の操作

**スタックは移動中も、内部のブロックをドラッグできる。**

- 上昇中のスタックに対する縦ドラッグは、**スタックローカル座標**で処理される（スタックは動いているが、指との相対位置は維持される）。
- 落下中も同様に操作可能。
- 地上スタックと空中スタックは**別の剛体**として扱い、**両者をまたぐドラッグはできない**。
- 同時に複数のスタックが存在しうる（別列で独立に点火した場合）。列が重なる場合は統合。
- **同時存在数の上限は3本**。上限に達している間、新規点火はスタックを増やさず最寄りのスタックに統合する。

### 5.2 点火猶予（マッチ「している最中」の操作）

- マッチ成立から `IGNITE_DELAY = 0.12s` の間、グループは**点火予兆状態**（白フラッシュ）になる。
- この猶予の間に**同色ブロックを隣接させると、点火グループに取り込まれる**（3マッチ → 4マッチ / 5マッチへの拡張）。
- 拡張のたびに猶予が `0.06s` 延長される（上限 `0.30s`）。
- 点火数 `k` が4以上なら**同時点火ボーナス** `× (1 + 0.25 * (k - 3))` が推力とスコアの両方に乗る。

### 5.3 連鎖カウント

- **連鎖が繋がる条件**: いずれかのスタックが**燃焼中**、または**最後の燃え尽きから `CHAIN_GRACE = 0.35s` 以内**に、新たな点火が発生すること。
- 条件を満たす点火ごとに `chain += 1`。
- **全スタックが着地・消滅し、かつ `CHAIN_GRACE` が経過した**時点で `chain = 0` にリセット。
- → **落下中に点火しても連鎖は途切れない。** 上昇 → 失速 → 落下中に再点火 → 再上昇、という「リブースト」で連鎖を伸ばせる。

### 5.4 連鎖倍率

```
chainMult(c) = 1 + 0.30 * (c - 1) + 0.05 * (c - 1)²
```

| 連鎖 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 倍率 | 1.00 | 1.35 | 1.80 | 2.35 | 3.00 | 3.75 | 4.60 | 5.55 |

- **推力への適用**: `min(chainMult(c), THRUST_MULT_CAP = 4.0)` を掛ける（青天井にすると上級者が無限に飛ぶため上限）。
- **スコアへの適用**: 上限なし。

連鎖が伸びるほど推力も上がるため、**連鎖は自己強化する**。序盤の1〜2連鎖が一番苦しく、3連鎖を超えると一気に楽になる——この非線形性が上達の手応えになる。

### 5.5 リブースト

落下中（`v < 0`）のスタックで点火に成功し、再び `v > 0` に転じた場合：

- **リブースト成立**。そのスタックの以降の獲得スコアに `× 1.5`。
- 専用SEとカメラシェイク、機体の再点火演出。
- 連鎖はリセットされない（5.3の通り）。

### 5.6 燃焼ブロックは「壁」

- 燃焼中のブロックは**掴めない**。
- 他のブロックのドラッグは、燃焼中ブロックを**通過できない**（列内で壁として機能する）。

→ 「どこで点火すると、次の連鎖の仕込みが詰まるか」という**位置取りの読み**が生まれる。
※ 本ルールは難易度への影響が大きいため、**チューニングノブとして残す**（P1でオン/オフ両方を実測比較）。

### 5.7 上昇中スタックへの落下ブロック

- 供給ブロックは、その列に上昇中スタックがある場合、**スタックの上端に着地する**（＝重量が増える）。
- 打ち上げが長引くほど重くなる。**連鎖は「早く繋ぐ」ほど有利**というプレッシャーがかかる。

---

## 6. 操作仕様

### 6.1 基本操作：縦ドラッグのみ

```
pointerdown  → 対象セル (c, r) のブロックを掴む（grabbed）
pointermove  → 移動量 dy から目標行 r' を算出
                r' = clamp(r + round(dy / cellPx), 0, columnTop)
                grabbed を r' に移動、間にあったブロックは逆方向に1セルずつシフト
                （= 列内セグメントの巡回シフト。メテオス準拠）
pointerup    → 掴み解除。マッチ判定は常時走っているため確定操作は不要
```

- **横方向の移動は一切ない。** 列をまたぐ移動は不可。
- ドラッグ開始しきい値 `DRAG_THRESHOLD = 0.35 cell`（誤タップ防止）。
- 横方向の指ブレは無視（`|dx|` は使用しない）。
- 列の最上段より上へは出せない。最下段より下へも出せない。

### 6.2 マルチタッチ

- **同時2本指まで**（`MAX_POINTERS = 2`）を受理。両親指で別々の列を同時操作できる。
- **指は2本、スタックは最大3本。** この非対称が本作の中心的な緊張であり、意図的な設計である（8.3.1）。
  「どのスタックを伸ばし、どれを失速させるか」の取捨選択が毎秒発生する。指を増やす方向の緩和（3本指対応等）は行わない。
- 3本目以降のポインタは無視。
- 2本が同じ列を掴んだ場合、**後から掴んだ方を破棄**する。

### 6.3 視認性への配慮（指でブロックが隠れる問題）

- 掴んでいるブロックは**指の上方 1.2セル分の位置にゴースト表示**（拡大 + 発光）。
- 掴んでいる列全体を**明度アップ + 幅わずかに拡張**して強調。
- 上昇中スタックは**輪郭発光 + 推進炎**で地上スタックと明確に区別。
- 点火予兆グループは**白フラッシュ + スケールパルス**。

### 6.4 補助表示

| 表示 | 内容 | 目的 |
| --- | --- | --- |
| **推力ゲージ** | 現在の `Ft / (Wt * G)` を 0〜2 で表示（1.0 に境界線） | 「今のまま届くか」を数値でなく直感で伝える |
| **到達予測ライン** | 現在の `v, a` から積分した予測最高到達点を盤面に点線表示 | **これが本作最大のオンボーディング装置。**「あと1連鎖で届く」が一目で分かる |
| **連鎖数** | 盤面中央フローティング | 手応えのフィードバック |

到達予測ラインは、ラインが排出ラインを越えた瞬間に色が変わる（赤→シアン）。
**「予測ラインを排出ラインまで押し上げるゲーム」**として初心者に言語化できる。

---

## 7. パラメータ定義

### 7.1 盤面

| 定数 | 値 | 説明 |
| --- | --- | --- |
| `COLS` | **6** | 列数。固定（8.3.1） |
| `ROWS` | 12 | 可視行数。端末が短い場合のみ 10 まで縮む（3.3） |
| `BUFFER_ROWS` | 2 | 天井上バッファ（供給ブロックの助走） |
| `DANGER_ROW` | `ROWS - 2` | 危険域演出の開始行 |
| `MAX_STACKS` | 3 | 同時に存在できる空中スタック数の上限（5.1） |
| `OVERFLOW_GRACE` | 1.5 s | 高さ超過からゲームオーバーまでの猶予 |

### 7.2 ブロック

| 種別 | 重量 | マッチ可 | 単体推力 | 備考 |
| --- | --- | --- | --- | --- |
| **通常** | 1.0 | ○ | 2.0 | 色 0〜4 |
| **重ブロック** | 2.5 | ○ | 3.5 | 重いが推力も大きい。ハイリスク燃料 |
| **ジャンク** | 1.5 | × | 0 | マッチ不可。**打ち上げて排出するしか消せない** |

- ジャンクは Lv.4 以降に混入（出現率 `min(0.05 + 0.01 * (Lv - 4), 0.12)`）。
- 重ブロックは Lv.3 以降に混入（出現率 `min(0.06 + 0.015 * (Lv - 3), 0.15)`）。

### 7.3 物理

| 定数 | 値 | 説明 |
| --- | --- | --- |
| `MATCH_MIN` | 3 | 最小マッチ数 |
| `THRUST_PER_BLOCK` | 2.0 | 通常ブロックの推力 |
| `BURN_TIME` | 0.60 s | 燃焼継続時間 |
| `IGNITE_DELAY` | 0.12 s | 点火猶予（最大 0.30 s まで延長） |
| `IGNITE_IMPULSE` | 12.0 | 初速インパルス係数 |
| `BASE_ACCEL` | 8.0 cell/s² | 加速度スケール |
| `G_BASE` | 1.0 | 基準重力 |
| `V_MAX_UP` | 14.0 cell/s | 上昇速度上限 |
| `V_MAX_DOWN` | 18.0 cell/s | 落下速度上限 |
| `CHAIN_GRACE` | 0.35 s | 連鎖継続猶予 |
| `THRUST_MULT_CAP` | 4.0 | 推力に乗る連鎖倍率の上限 |
| `FALL_SPEED` | 16.0 cell/s | 供給ブロックの自然落下速度 |

### 7.4 操作

| 定数 | 値 |
| --- | --- |
| `DRAG_THRESHOLD` | 0.35 cell |
| `MAX_POINTERS` | 2 |
| `INPUT_BUFFER` | 2 frames（直近入力の先行受理） |

---

## 8. 難易度カーブ

### 8.1 レベル進行

6列固定のため、**幅補正の概念は廃止**し、以下をそのまま使う絶対値とする。

| Lv | 到達条件（経過秒） | 色数 | `dropRate`（個/秒） | `G` |
| --- | --- | --- | --- | --- |
| 1 | 0 | 3 | 1.2 | 1.00 |
| 2 | 30 | 4 | 1.5 | 1.00 |
| 3 | 75 | 4 | 1.8 | 1.05 |
| 4 | 120 | 4 | 2.2 | 1.10 |
| 5 | 165 | 5 | 2.6 | 1.15 |
| 6 | 225 | 5 | 3.0 | 1.20 |
| 7 | 285 | 5 | 3.4 | 1.30 |
| 8 | 345 | 5 | 3.8 | 1.40 |
| 9+ | +60 ごと | 5 | +0.3（上限 `DROP_RATE_MAX = 4.8`） | +0.05（上限 1.8） |

- Lv1（3色）は**30秒で抜ける**。6列 × 3色は横マッチが成立しすぎるため、長居させない。
- 4色への移行を早め、5色到達も 165秒と早い。6列は横3マッチに「6列中どこか3連続」でよく、色数でしか締められないため。
- 1列あたりの供給は Lv1 で 0.2個/秒。何も操作しなければ約60秒で1列が天井に達する計算。

### 8.2 供給アルゴリズム

**列の選択**（詰みの偏りを防ぐ）

```
weight(c) = (maxHeight - height(c) + 1) ^ 1.5
```
重み付き抽選。低い列に偏って供給され、盤面が極端に傾くのを防ぐ。

**色の選択**（色枯れを防ぐ）

- **バッグ方式**: `colors × 2` 個のバッグをシャッフルして消費、空になったら再生成。
- **即席3マッチ禁止**: 着地時点でプレイヤー操作なしに3マッチが成立する色は、確率 80% で引き直し（1回のみ）。棚ぼたを減らし、操作の価値を保つ。

**決定性**: すべての抽選は**シード付きPRNG（xorshift128）**を通す。デイリーモード・リプレイ・自動テストのために乱数は完全に再現可能とする。

### 8.3 6列固定の設計根拠と難易度レバー

#### 8.3.1 なぜ6列のみか（4列・5列を廃止した理由）

4列は**一見難しい**。横3マッチに「4列中3列」を揃える必要があり、色数が増えると横マッチはほぼ成立しなくなる。
しかし数字上の難度と、**遊びとしての手応え**は別だった。

| 4列の問題 | 内容 |
| --- | --- |
| 選択肢が少なく最善手が自明 | 1手あたりの候補が少なく、読みではなく作業になる |
| 打ち上げると盤面がほぼ空になる | 横3マッチが盤面の75%を浮かせる。失敗が蓄積せず、リカバリが容易すぎる |
| 並行処理が発生しない | スタックは実質1本。「どれを先に処理するか」の判断が生まれない |
| **縦マッチ偏重** | 横が作りにくいぶん縦3マッチに頼る。縦マッチは上に乗る重量が小さく、**単発で排出できてしまう** |

最後の項目が決定的である。**「単発では届かない」という本作の前提（4.4）が、4列では崩れる。**
4列の狭さは緊張を生む代わりに、本作の核であるアクティブ連鎖を要求しなくなる方向に働いていた。

6列では以下が成立する。

- 横3マッチが浮かせるのは盤面の**半分**。残り3列で**並行して次を仕込む**必要がある。
- 打ち上げ後も半分の盤面が残るため、**失敗が蓄積する**。リカバリは可能だが無料ではない。
- 最大3本のスタックが同時に存在しうるが、**指は2本しかない**（6.2）。
- 横マッチが成立しやすいぶん、**色数によるコントロールが効く**（8.1でLv2から4色にできるのはこのため）。

> **「指2本 vs スタック3本」が本作の上達曲線の中心**であり、これは6列でしか成立しない。
> 5列は4列と6列の中間として意味を持ちうるが、**選択肢を残すこと自体が仕様と調整を薄める**ため廃止した。

#### 8.3.2 難易度レバー

盤面幅を難易度レバーとして**使わない**。残る調整軸は以下。

| レバー | 効き方 | 使いどころ |
| --- | --- | --- |
| **供給速度 `dropRate`** | 盤面が埋まる速度。最も直接的 | レベル進行の主軸（8.1） |
| **色数** | マッチの成立しやすさ。3→4で体感が最も大きく変わる | 序盤の緩急 |
| **ジャンクブロック** | 打ち上げでしか消せない死に重量 | 中盤以降の圧（7.2） |
| **重ブロック** | 重いが推力も大きい。読みを要求 | 上級者向けの味付け |
| **重力 `G`** | 打ち上げの成功難度そのもの | 惑星ごとの個性（8.4） |
| **同時スタック上限** | 3固定。**変更しない**（本作の核のため） | — |

#### 8.3.3 速度プリセット（幅による難易度選択の代替）

幅で難易度を下げる手段を失ったため、**速度プリセット**で代替する。
ルールは一切変わらない（列数も同時スタック上限も同じ）ので、上達したらそのまま標準へ移行できる。

| プリセット | `dropRate` | `G` | 対象 |
| --- | --- | --- | --- |
| **ゆっくり** | ×0.70 | ×0.85 | 初心者。アクセシビリティ用途（11.2）を兼ねる |
| **標準** | ×1.00 | ×1.00 | 既定 |
| **高速** | ×1.30 | ×1.00 | 上級者。Lv3相当の盤面から開始する |

- **記録は「標準」のみをランキング・実績の対象**とする。ゆっくり／高速のスコアは保存するが別枠。
- リザルトには常にプリセット名を表示する。
- プリセットはプレイ中に変更できない。

#### 8.3.4 6列に起因する供給の調整

6列は横3マッチが成立しやすく、**プレイヤーが意図しない棚ぼた点火**が増える。
これは「意図して連鎖を組む」という設計意図を薄めるため、供給側で抑える。

- 8.2 の「即席3マッチ禁止」の引き直し確率を **80% → 90%** に引き上げる。
- 加えて、**横方向に同色が既に2連続している位置**への同色供給を、確率 60% で引き直す（1回のみ）。
- いずれも `IGNITE_DELAY`（5.2）中の**プレイヤーによる意図的な拡張は妨げない**。供給側だけを抑制する。

### 8.4 重力のバリエーション（スターツアー）

惑星ごとに `G` と `dropRate` の組み合わせを変え、同じルールで別の「感触」を作る。

| 惑星 | `G` | `dropRate` | 性格 |
| --- | --- | --- | --- |
| ルナ | 0.70 | 1.4 | よく飛ぶ。長い連鎖の練習場 |
| テラ | 1.00 | 1.2 | 標準 |
| ネビュラ | 1.00 | 2.4 | 飛ぶが降る量が多い。処理速度勝負 |
| ゴリアテ | 1.60 | 0.9 | ほぼ飛ばない。高連鎖必須の詰将棋 |
| ブラックホール | 1.30→1.9 | 1.6 | 時間経過で重力が上がる。後半ほど苦しい |

※ 表の `dropRate` は6列での実値（8.1と同じスケール）。速度プリセット（8.3.3）の係数はこの上に掛かる。

---

## 9. スコア

### 9.1 点火スコア

```
点火1回あたり:
  base    = k * 100
  sameHit = 1 + 0.25 * max(0, k - 3)        // 同時点火ボーナス
  score  += base * chainMult(chain) * sameHit * reboostMult
```
`reboostMult` は当該スタックがリブースト成立済みなら 1.5、それ以外は 1.0。

### 9.2 排出スコア

```
排出1回あたり:
  score += (排出ブロック数) * 50 * chainMult(そのスタックの最大連鎖)
  score += (ジャンク排出数) * 150                  // ジャンク処理の報酬
```

### 9.3 評価（モード終了時）

| 項目 | 内容 |
| --- | --- |
| 総スコア | 上記の合計 |
| 最大連鎖 | セッション中の `chain` 最大値 |
| 総排出数 | 画面外へ送ったブロック数 |
| 失速率 | 失速落下したスタック数 / 総点火スタック数 |
| リブースト回数 | 成立回数 |

ランクは総スコアではなく **最大連鎖 × 失速率** で決める（＝「たくさん遊んだ人」ではなく「上手い人」を評価する）。

---

## 10. モード（すべてソロ）

| モード | 内容 | 終了条件 | 狙い |
| --- | --- | --- | --- |
| **スターツアー** | 惑星を巡るステージ制。各惑星に3つの課題（例:「4連鎖を2回」「ジャンク15個排出」「90秒生存」） | 課題達成 or ゲームオーバー | メイン導線。ルールを段階的に教える |
| **エンドレス** | 8.1のテーブルに従って無限に難化 | ゲームオーバー | 腕試し。記録更新の中心 |
| **タイムアタック** | 90秒固定。ゲームオーバーなし（溢れたら最上段が強制排出されペナルティ） | 時間切れ | 短時間で回せる。通勤1本用 |
| **チャレンジ** | 供給停止・固定盤面。「規定手数以内に全消し」等 | 達成 or 手詰まり | 連鎖の理屈を頭で理解させる。リトライ即時 |
| **デイリー** | 日付シード固定のエンドレス。全員同じ盤面 | ゲームオーバー | 再訪理由。ローカル記録 + 将来のランキング基盤 |

**全モード6列固定。** 速度プリセット（8.3.3）はエンドレス / タイムアタックで選択可、
スターツアー / チャレンジ / デイリーは「標準」固定（課題と作問が速度前提で設計されるため）。

### 10.1 チュートリアル（スターツアー内に統合）

1. **降ってくる / 縦に動かせる** — マッチなし、操作だけ
2. **3つ揃うと飛ぶ** — 最上段だけ、必ず排出できる配置
3. **重いと飛ばない** — わざと失速させる。到達予測ラインを初出し
4. **飛んでる途中も動かせる** — 上昇中に指を置かせ、2連鎖を成立させる（**ここが本作の山場**）
5. **落ちながらでも動かせる** — リブーストを体験させる

チュートリアルは**説明文より先に手を動かさせる**。テキストは各ステップ1行まで。

**6列前提の作り方**: 序盤のステップで盤面を狭めることはできないため、
**供給を止めて使う列だけにブロックを配置する**（他の列は空のまま）。
ステップ1〜3は中央2〜3列のみ、ステップ4で4列、ステップ5で全6列に広げ、
最後に「指は2本、スタックは3本」の状況を意図的に作って体験させる。

---

## 11. 演出・UX

### 11.1 フィードバック設計

| イベント | 視覚 | 聴覚 | 触覚 |
| --- | --- | --- | --- |
| 掴む | ブロック拡大 + ゴースト | 小さなクリック | 軽い振動（10ms） |
| 点火予兆 | 白フラッシュ + スケールパルス | チャージ音（上昇ピッチ） | — |
| 点火 | 発光 + 推進炎パーティクル | 連鎖数に応じて音階上昇（ドレミファソラシド） | 中振動（20ms） |
| 上昇 | 尾を引くトレイル、背景の星が下に流れる | ロケット低音ループ | — |
| 失速 | 炎が消え、色が褪せる、わずかにスロー | 音がしぼむ | — |
| リブースト | 画面フラッシュ + カメラシェイク | 派手な再点火音 | 強振動（40ms） |
| 排出 | 画面上端に衝撃波、スコアが飛ぶ | 爆発 + 高音のきらめき | 強振動（40ms） |
| 危険域 | 外周の赤パルス、盤面の微振動 | BGMにローパス + 心拍音 | 断続振動 |

- 連鎖の音階上昇は**パネポン同様、連鎖の手応えを最も強く伝える要素**。実装優先度は高い。
- バイブレーションは `navigator.vibrate` を使用。iOS Safari非対応のため**必須機能にしない**。設定でOFF可。

### 11.2 アクセシビリティ

- **色覚対応必須**: 各色に固有の**形状シンボル**（●／▲／■／◆／★）を持たせる。色だけで判別させない。
- 「高コントラストモード」（彩度を落とし輪郭を強調）を設定に用意。
- 「演出控えめモード」（画面フラッシュ・シェイク無効）。光過敏性への配慮。
- 速度プリセット「ゆっくり」（8.3.3）を設定から常時選択可能。難易度調整とアクセシビリティを兼ねる。
- **両手持ちが必須**（3.3）であることは、運動機能に関する制約として設定画面とストア説明に明記する。

### 11.3 リトライ導線

- ゲームオーバーからリトライまで**2タップ以内・1.5秒以内**。
- リザルトは「最大連鎖」を最大表示。総スコアはその下。
- 直前のプレイの最大連鎖シーンを3秒リプレイで再生（決定性シミュレーションを利用）。

---

## 12. 技術構成

### 12.1 スタック

| 層 | 採用 | 理由 |
| --- | --- | --- |
| 言語 | TypeScript（strict） | 物理パラメータと状態遷移の型安全性 |
| ビルド | Vite | 高速HMR。パラメータ調整のイテレーション速度が生命線 |
| 描画 | PixiJS v8（WebGL / WebGPU） | パーティクルとトレイルが安価。Canvas2Dフォールバックあり |
| 音 | Web Audio API 直叩き（薄いラッパー自作） | 連鎖の音階制御にスケジューリング精度が必要。Howlerでは粒度不足 |
| 状態 | 自前の純粋関数ステートマシン | DOM非依存。Node上でヘッドレステスト可能 |
| テスト | Vitest | シミュレーションのリグレッションテスト |
| 配信 | 静的ホスティング（Cloudflare Pages / Vercel） + PWA | サーバー不要 |
| 永続化 | localStorage（v1）／IndexedDB（リプレイ保存時） | v1はバックエンドなし |

### 12.2 アーキテクチャ原則

```
┌──────────────┐
│  input/      │ PointerEvent → Command[]（純粋なデータ）
└──────┬───────┘
       ↓
┌──────────────┐
│  sim/        │ GameState + Command[] + dt → GameState
│              │ ★ 純粋関数。DOM・時刻・Math.random を一切参照しない
└──────┬───────┘
       ↓
┌──────────────┐
│  render/     │ GameState → 描画（状態を書き換えない）
└──────────────┘
```

- **固定タイムステップ 60Hz**。`requestAnimationFrame` からアキュムレータで固定ステップを回し、描画は補間。
- 乱数はすべて `GameState.rng`（xorshift128）から取得。`Math.random` の使用を ESLint で禁止。
- **列数は仕様としては6固定だが、コード上はマジックナンバーにしない。** `sim` 層は `state.board.cols` / `state.board.rows` のみを参照する。
  端末フォールバックで `rows` が変わるため、どちらにせよ盤面サイズ非依存に書く必要がある。
  リプレイ・デイリーのシードデータには `rows` と `speed` を必ず含める（欠けると再現できない）。
- → **同じシード + 同じ入力列 = 同じ結果**。リプレイ、デイリー、自動バランステストがすべてこの上に乗る。

### 12.3 主要データ構造

```ts
type Color = 0 | 1 | 2 | 3 | 4;
type BlockKind = 'normal' | 'heavy' | 'junk';

interface Block {
  id: number;
  kind: BlockKind;
  color: Color;          // junk は色を持つが照合対象外
  burnState: 'idle' | 'pending' | 'burning';
  pendingUntil?: number; // 点火猶予の終了時刻
  burnUntil?: number;    // 燃焼終了時刻
  groupId?: number;      // 所属する点火グループ
}

/** 空中の剛体（上昇中 or 落下中） */
interface Stack {
  id: number;
  cols: Map<number, Block[]>;  // 列index → 下から順のブロック列
  baseRow: number;             // 点火時点の最下段行
  y: number;                   // baseRow からのオフセット [cell]（float）
  v: number;                   // 速度 [cell/s]
  burns: Burn[];               // 燃焼中グループ
  maxChain: number;
  reboosted: boolean;
}

interface Burn {
  groupId: number;
  thrust: number;   // k * THRUST_PER_BLOCK * min(chainMult, CAP) * sameHit
  until: number;
}

interface BoardConfig {
  cols: 6;                          // 固定。定数直書きを避けるため状態として保持する
  rows: number;                     // 通常 12。短い端末で 10 まで縮む（3.3）
  maxStacks: 3;                     // 同時スタック上限
  speed: 'slow' | 'normal' | 'fast';// 速度プリセット（8.3.3）
}

interface GameState {
  t: number;                        // 経過秒（固定ステップの積算）
  board: BoardConfig;               // ★ sim 層は 6 を直書きせず常にこれを参照する
  rng: Rng;
  ground: Block[][];                // [col][row] 下から順。地上スタック
  falling: FallingBlock[];          // 供給中で未着地のブロック
  stacks: Stack[];
  chain: number;
  chainExpiresAt: number;
  score: number;
  level: number;
  params: LevelParams;              // dropRate, colors, G ...
  phase: 'playing' | 'overflow' | 'gameover';
  overflowSince?: number;
}
```

### 12.4 1フレームの更新順（決定性の担保に重要）

```
1. 入力コマンド適用      … ドラッグによる列内シフト（地上 / 各スタック）
2. 供給                  … dropRate に基づく新ブロック生成（rng）
3. 落下ブロックの前進    … 着地判定 → ground or stack 上端へ
4. スタックの積分        … Ft / Wt / G から a, v, y を更新
5. 燃え尽き処理          … burnUntil 到達グループを消滅、重量更新
6. 排出判定              … 最下段が排出ラインを越えたスタックを消滅 + 加点
7. 着地判定              … v <= 0 かつ下端が地上に接触 → ground へ統合
8. マッチ判定            … ground と各 stack について独立に走査
9. 点火処理              … pending → burning。連鎖判定、推力とスコアの確定
10. 危険 / ゲームオーバー判定
11. （描画は別ループ。補間のみ）
```

> **8 を 1〜7 の後に置くのが要点。** 移動中の操作・移動そのものによる隣接変化が、同一フレーム内で点火に反映される。これがアクティブ連鎖の成立条件。

### 12.5 パフォーマンス方針

- ブロック数の上限は 6×14 + 空中分 ≒ 110 程度。描画負荷の主因は**パーティクル**なので、プール化 + 上限（同時 300）を設ける。
- `sim` 層でのオブジェクト生成を最小化（フレームごとの GC を避ける）。配列は再利用。
- テクスチャは単一アトラスにまとめ、ドローコールを 10 以下に抑える。
- 低性能端末検出時（連続10フレームで16.7ms超過）は自動で「演出控えめモード」に降格。

---

## 13. 開発フェーズ

| Phase | 期間目安 | 成果物 | 完了判定 |
| --- | --- | --- | --- |
| **P0: コア検証** | 2週 | 4×12盤面、縦ドラッグ、点火→上昇→排出/落下。演出は矩形と単色のみ。**パラメータをURLクエリで即変更できるデバッグUI** | 「上昇中に2連鎖を繋ぐ」が**自分で楽しいと感じられるか**。ここがNoなら数値を作り直す。演出には一切進まない |
| **P1: 連鎖システム** | 1週 | 点火猶予、連鎖カウント、リブースト、到達予測ライン、燃焼ブロック壁ルールのA/B | 予測ラインを見て連鎖を狙う行動が、未経験者に自然発生するか |
| **P2: 難易度とモード** | 1週 | レベルテーブル（8.1）、**速度プリセット**、供給の棚ぼた抑制（8.3.4）、エンドレス / タイムアタック / チャレンジ | エンドレス（標準）の初見平均生存時間が 2〜4分に収まるか。**3本目のスタックが出る頻度が「毎回捨て札が出る」程度に収まっているか** |
| **P3: 演出と音** | 1.5週 | パーティクル、連鎖音階、振動、リザルト、チュートリアル | 音を消しても遊べるが、音があると明確に気持ちいいか |
| **P4: 仕上げ** | 1週 | PWA、パフォーマンス最適化、アクセシビリティ設定、スターツアー、デイリー | 実機（SE2 / 中位Android）で60fps、入力遅延50ms以内 |

### 13.1 P0 で必ず用意するもの

パラメータ調整の回数が品質を決めるため、以下は**P0で作り切る**。

- 全定数のランタイム変更UI（スライダー + URLクエリ + localStorage永続化）
- シード指定でのリスタート
- **行数の即時切替**（10〜12。端末フォールバック検証用。盤面サイズ非依存の実装を最初から強制する意味もある）
- **同時スタック上限の即時切替**（1〜4。「指2本 vs スタック3本」が成立する上限が本当に3かをP0で確かめる）
- スロー再生（0.25x）とコマ送り
- 推力 / 重量 / 加速度 / 速度のリアルタイム数値表示

---

## 14. リスクと対策

| リスク | 深刻度 | 対策 |
| --- | --- | --- |
| **上昇中の操作が難しすぎる**（本作の全て） | 高 | 到達予測ライン、スタックの輪郭発光、`V_MAX_UP` を抑えて視線が追える速度に。P0で未経験者3人に触らせて判断 |
| **指でブロックが隠れる** | 高 | ゴースト表示（指の上1.2セル）、列全体のハイライト、親指セーフゾーンの確保 |
| **物理が理解されない** | 中 | チュートリアルで**わざと失速させる**。推力ゲージと予測ラインで数値を直感化 |
| **6列は初心者に情報量が多すぎる** | 高 | 幅で易しくする手段がないため、**速度プリセット「ゆっくり」（8.3.3）が唯一の緩和策**になる。チュートリアルで列を段階的に開ける（10.1）。P2で初見の離脱率を実測し、緩和が足りなければ「ゆっくり」をさらに下げる |
| **片手プレイを失う** | 中 | 6列固定の既知の代償。承知のうえで受け入れる。通勤・吊革での短時間プレイは**タイムアタック（90秒）を座って遊ぶ**用途に寄せ、片手前提の訴求はしない |
| **端末が短く6列が窮屈** | 中 | 列数は減らせないため行数で吸収する（3.3のフォールバック）。`ROWS=10` でも `cell < 36px` の端末は警告のみでブロックしない |
| **棚ぼた点火が多すぎて連鎖を「組む」感覚が薄れる** | 中 | 8.3.4の供給抑制（引き直し90% + 横2連続への同色抑制）。それでも多ければ色数の到達をさらに早める |
| **iOS Safari の入力遅延・スクロール干渉** | 中 | `touch-action: none`、`passive: false`、Pointer Events統一。実機での遅延計測をCIに組み込まない代わりP0で手動測定 |
| **連鎖倍率が青天井で崩壊** | 中 | 推力側に `THRUST_MULT_CAP = 4.0`。上昇中スタックへの落下ブロック着地（5.7）が自然なブレーキになる |
| **点火猶予がストレスになる** | 低 | `IGNITE_DELAY` は0にもできる設計にし、P1で実測比較 |
| **PWAのiOS制約**（インストール導線の弱さ） | 低 | ブラウザ直アクセスで完結する前提。ホーム画面追加は任意の付加価値に留める |

---

## 15. 未決事項（今後の判断ポイント）

| # | 論点 | 判断時期 |
| --- | --- | --- |
| 1 | 燃焼ブロックの「壁」ルール（5.6）を採用するか | P1 実測 |
| 2 | 点火猶予（5.2）の秒数、および廃止の可否 | P1 実測 |
| 3 | 同時スタック上限が3で妥当か。本作の核そのものなので、2に下げると別のゲームになる。P0のデバッグUIで1〜4を実測して確定させる | P0〜P1 |
| 4 | 6列で初心者が離脱しないか。「ゆっくり」だけで足りない場合、チュートリアル内の**列開放を本編の序盤数分まで延長**する案がある（盤面幅の選択肢を戻すことはしない） | P2 |
| 4-b | 片手プレイを切り捨てたままでよいか。救済するなら縦画面のまま**盤面を画面下寄せ**にして到達性を稼ぐ程度に留める（列数は動かさない） | P3 |
| 4-c | チャレンジ（作問モード）でのみ、狭い盤面を問題データとして許すか。連鎖の理屈を教えるのに有効だが、本編と操作感が食い違う | P2 |
| 5 | アートディレクション（宇宙×ロケット以外の案。例: 深海からの浮上、地下からの脱出） | P3 |
| 6 | リプレイ共有（決定性シミュレーションの副産物として安価。SNS導線になりうる） | P4以降 |
| 7 | オンラインランキング（バックエンド必要。v1スコープ外） | v2 |
| 8 | 対戦モード（ソロで手応えが出てから。おじゃま送り＝相手の塊を重くする、が自然な設計） | v2 |

---

## 付録A: 用語

| 用語 | 定義 |
| --- | --- |
| **点火** | 同色3つ以上の連続が成立し、燃焼状態に入ること |
| **スタック** | 空中にある剛体。上昇中または落下中のブロックの塊 |
| **地上スタック** | 盤面に積まれている、動いていないブロック群 |
| **排出** | スタックが排出ラインを越えて画面外へ消滅すること（＝クリア） |
| **失速** | 推力不足で上昇速度が0以下になり、排出できずに落下すること |
| **リブースト** | 落下中のスタックで再点火し、再上昇に転じること |
| **アクティブ連鎖** | 上昇中・落下中のスタック内部を操作して連鎖を継続すること。本作の中核 |
| **点火猶予** | マッチ成立から実際の点火までの猶予。この間にグループを拡張できる |
| **速度プリセット** | ゆっくり / 標準 / 高速。`dropRate` と `G` に係数を掛けるだけで、ルールは変えない |

## 付録B: 数値の検算シート

`BASE_ACCEL=8, G=1.0, THRUST_PER_BLOCK=2.0, IGNITE_IMPULSE=12, BURN_TIME=0.6`

6列・`ROWS=12` 前提。速度プリセットは `G` に係数を掛けるため、「ゆっくり」では `G=0.85` となり全ケースが一段楽になる。

| ケース | k | 上に乗る数 | Wt | Ft | a | 初速 v | 判定 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 最上部3マッチ | 3 | 0 | 3.0 | 6.0 | +8.0 | 12.0 | 即排出 |
| 上に2個 | 3 | 2 | 5.0 | 6.0 | +1.6 | 7.2 | 排出可（余裕あり）|
| 上に5個 | 3 | 5 | 8.0 | 6.0 | −2.0 | 4.5 | 約3セルで失速 → **要連鎖** |
| 上に5個 + 2連鎖 | 3+3 | 5 | 5.0※ | 8.1 | +5.0 | +7.2 | 加速して排出 |
| 上に5個 + 4マッチ | 4 | 5 | 9.0 | 10.0 | +0.9 | 5.3 | ぎりぎり排出 |
| 上に8個（高重力 G=1.6）| 3 | 8 | 11.0 | 6.0 | −8.4 | 3.3 | ほぼ即失速 → **3連鎖以上必須** |

※ 1回目の燃焼グループが燃え尽きた後の重量

---

*本書は開発の進行に伴い更新する。特に第7章のパラメータは P0 の実測で全面的に書き換わる前提で扱うこと。*
